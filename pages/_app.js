if (process.browser) {
  window.exampleRemote.init(__webpack_share_scopes__.default);
}
import '../styles/globals.css'

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
